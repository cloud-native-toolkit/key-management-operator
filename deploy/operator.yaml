apiVersion: apps/v1
kind: Deployment
metadata:
  name: key-management-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      name: key-management-operator
  template:
    metadata:
      labels:
        name: key-management-operator
    spec:
      serviceAccountName: key-management-operator
      containers:
        - name: key-management-operator
          # Replace this with the built image name
          image: REPLACE_IMAGE
          command:
          - key-management-operator
          imagePullPolicy: Always
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "key-management-operator"
